function {{JS_PREFIX}}_apply_blocks(blocked_users) {
  // hooray from filesystem! edited
  console.log('apply_blocks starting at '+new Date()+', blocked_users='+JSON.stringify(blocked_users));
}

function {{JS_PREFIX}}_consider_block(comments, blocked_users) {
  // hooray from filesystem!
  console.log('consider_block starting, blocked_users='+JSON.stringify(blocked_users));
}
